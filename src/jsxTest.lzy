import * as LzyReact from '../lzy-React/out/index_V2'
import { render } from '../lzy-React/out/index_V2'



const Child = () => <div>子组件</div>


function App() {
    return (
        <LZY-TEMPLATE>
            123
            <Child />
            <div id={1} fn={() => { }} onClick={() => { console.log('点击') }}>div</div>
        </LZY-TEMPLATE>
    )
}

// 执行渲染
render(App)
